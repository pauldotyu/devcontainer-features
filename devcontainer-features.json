{
    "features": [
        {
            "id": "extension",
            "name": "Azure CLI extension name",
            "options": {
                "extension": {
                    "type": "string",
                    "proposals": [ "containerapp", "containerapp-compose" ],
                    "default": "containerapp-compose",
                    "description": "Enter the name of the Azure CLI extension you wish to add"
                }
            },
            "include": ["ubuntu"]
        }
    ]
}
